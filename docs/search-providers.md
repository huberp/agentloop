# Search Providers

Agentloop supports these search modes:

- `duckduckgo` via `web_search`
- `tavily` when `TAVILY_API_KEY` is configured
- `none` to disable provider-backed search

Set `WEB_SEARCH_PROVIDER` in `.env` or the environment.
