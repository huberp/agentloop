# Graph Runtime Removal Notes

The old orchestration layer was replaced by PydanticAI agents plus explicit plan execution helpers. Planning is now driven by structured `Plan` output and step delegation instead of a graph runtime.
