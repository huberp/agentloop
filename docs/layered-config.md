# Layered Configuration

Configuration precedence is:

1. CLI or constructor overrides
2. environment variables
3. `.env`
4. `docs/examples/user-config.json`
5. `docs/examples/repo-config.json`
6. defaults in `Settings`
